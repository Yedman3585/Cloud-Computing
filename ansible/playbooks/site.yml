---
- name: Validate firewall inventory contract
  hosts: firewalls
  gather_facts: false
  vars_files:
    - ../group_vars/all.yml
    - ../group_vars/firewalls.yml
  tasks:
    - name: Validate firewall inventory and rule references
      inventory_validate:
        firewall_node_names: "{{ groups['firewalls'] | default([]) }}"
        firewall_hostvars: "{{ hostvars }}"
        firewall_objects: "{{ firewall_objects }}"
        firewall_rules: "{{ firewall_rules }}"
        keepalived_vrrp_instances: "{{ keepalived_vrrp_instances }}"
        required_firewall_count: "{{ firewall_required_node_count }}"
      run_once: true
      delegate_to: localhost
      become: false

- name: Configure scalable firewall nodes
  hosts: firewalls
  become: true
  vars_files:
    - ../group_vars/all.yml
    - ../group_vars/firewalls.yml
  roles:
    - common
    - firewall
    - keepalived
    - conntrackd

- name: Configure routing through the firewall cluster VIPs
  hosts: test_clients:backend_servers
  become: true
  gather_facts: false
  roles:
    - routing
